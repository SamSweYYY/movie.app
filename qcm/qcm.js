import questions from './questions.js'

const quiz = document.querySelector('.quiz')

questions.forEach((item)  =>  {
    const id = item.id
    const question = item.question
    const solution = item.correction

})